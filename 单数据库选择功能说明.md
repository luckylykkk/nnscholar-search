# 单数据库智能选择功能说明

## 概述

根据您的要求，系统已经改进为**只选择一个最合适的数据库**，而不是推荐多个数据库。这样可以提高精准度，避免用户选择困难，并确保每次搜索都使用最适合的数据源。

## 改进后的工作流程

### 1. 用户输入研究主题
用户在搜索框中输入自然语言的研究内容，例如：
- "糖尿病的药物治疗研究"
- "深度学习算法优化"
- "气候变化政策分析"

### 2. 智能分析并选择最佳数据库
系统使用DeepSeek AI分析用户输入，**严格从5个指定数据库中选择一个最适合的**：

#### 数据库选择规则：
- **医学、临床、生物医学相关** → 选择 **PubMed**
- **物理学、数学、计算机科学理论** → 选择 **arXiv**
- **计算机科学应用、AI、机器学习** → 选择 **Semantic Scholar**
- **跨学科、综合性、社会科学** → 选择 **OpenAlex**
- **需要开放获取全文** → 选择 **DOAJ**

### 3. 生成数据库特定的检索策略
系统为选定的数据库生成专门优化的检索式：

#### PubMed检索式示例：
```
用户输入: "糖尿病的药物治疗研究"
检索式: "Diabetes Mellitus"[MeSH] AND "Drug Therapy"[MeSH] AND "Therapeutic Use"[Subheading]
```

#### arXiv检索式示例：
```
用户输入: "深度学习算法优化"
检索式: cat:cs.LG AND "deep learning" AND optimization
```

#### Semantic Scholar检索式示例：
```
用户输入: "机器学习在图像识别中的应用"
检索式: "machine learning" AND "image recognition" AND "computer vision"
```

## 技术实现

### 1. 改进的AI提示词

```python
analysis_prompt = f"""
作为学术搜索专家，请分析以下用户输入的研究内容，并从指定的数据库列表中推荐最适合的单个学术数据库。

用户输入："{user_input}"

**重要：必须从以下5个数据库中选择一个，不能推荐其他数据库：**

1. PubMed - 生物医学和医学文献数据库
2. arXiv - 物理学、数学、计算机科学等预印本论文
3. Semantic Scholar - AI驱动的学术搜索
4. OpenAlex - 全学科综合数据库
5. DOAJ - 开放获取期刊

请严格按照以下格式输出：

**研究领域识别：** [识别的主要研究领域]
**最佳数据库：** [必须从上述5个数据库中选择一个]
**推荐理由：** [详细说明为什么这个数据库最适合]
**检索重点：** [指出检索的关键概念和重点]
"""
```

### 2. 增强的数据库名称识别

系统能够识别AI返回的各种数据库名称表达方式：

```python
db_mapping = {
    'pubmed': 'pubmed',
    'pub med': 'pubmed',
    'medline': 'pubmed',
    'arxiv': 'arxiv',
    'ar xiv': 'arxiv',
    'semantic scholar': 'semantic_scholar',
    'semanticscholar': 'semantic_scholar',
    'openalex': 'openalex',
    'open alex': 'openalex',
    'doaj': 'doaj',
    'directory of open access journals': 'doaj'
}
```

### 3. 数据库特定的检索式生成

每个数据库都有专门的检索式转换逻辑：

#### PubMed检索式特点：
- 使用MeSH主题词：`"Diabetes Mellitus"[MeSH]`
- 字段限定符：`[Title/Abstract]`, `[Author]`
- 布尔逻辑：`AND`, `OR`, `NOT`

#### arXiv检索式特点：
- 分类代码：`cat:cs.LG`, `cat:physics.optics`
- 字段搜索：`ti:"title"`, `abs:"abstract"`
- 学科特定术语

#### Semantic Scholar检索式特点：
- 精确短语：`"machine learning"`
- 布尔操作符：`AND`, `OR`
- 技术术语组合

#### OpenAlex检索式特点：
- 通用学术术语
- 跨学科关键词
- 广泛覆盖的表达

#### DOAJ检索式特点：
- 开放获取期刊术语
- 国际化表达
- 高质量学术概念

## API接口

### `/api/analyze_query` (POST)
分析用户查询并推荐最佳数据库

**请求示例：**
```json
{
    "query": "糖尿病的药物治疗研究"
}
```

**响应示例：**
```json
{
    "success": true,
    "query": "糖尿病的药物治疗研究",
    "detected_field": "医学",
    "recommended_database": "pubmed",
    "search_strategy": "\"Diabetes Mellitus\"[MeSH] AND \"Drug Therapy\"[MeSH]",
    "analysis_info": {
        "analysis_text": "AI分析结果...",
        "selection_method": "ai_analysis"
    }
}
```

## 优势

### 1. 精准性
- 每次只推荐一个最适合的数据库
- 避免用户选择困难
- 确保搜索结果的相关性

### 2. 专业性
- 每个数据库都有专门优化的检索式
- 充分利用各数据库的特色功能
- 提高检索效率和质量

### 3. 智能化
- AI驱动的领域识别
- 自动数据库选择
- 智能检索式转换

### 4. 一致性
- 统一的用户体验
- 标准化的输出格式
- 可预测的行为

## 测试验证

系统包含完整的测试套件：

```bash
python test_single_database_selection.py
```

测试覆盖：
- 不同领域的查询准确性
- 数据库特定检索式生成
- 错误处理和回退机制

## 使用示例

### 医学查询
```
输入: "冠心病的介入治疗"
推荐: PubMed
检索式: "Coronary Disease"[MeSH] AND "Angioplasty"[MeSH]
```

### 计算机科学查询
```
输入: "卷积神经网络图像分类"
推荐: arXiv
检索式: cat:cs.CV AND "convolutional neural network" AND "image classification"
```

### 跨学科查询
```
输入: "人工智能在教育中的应用"
推荐: OpenAlex
检索式: artificial intelligence education application learning technology
```

## 总结

改进后的系统实现了：

1. ✅ **单一数据库选择**：每次只推荐一个最适合的数据库
2. ✅ **强制约束**：AI必须从指定的5个数据库中选择
3. ✅ **专业检索式**：为每个数据库生成专门优化的检索策略
4. ✅ **智能分析**：基于用户输入自动识别最佳数据源
5. ✅ **完整测试**：包含验证功能正确性的测试套件

这种设计确保了用户只需要输入自然语言查询，系统就能自动选择最合适的数据库并生成专业的检索策略，大大简化了学术文献检索的复杂性。
