# 多数据库学术文献检索系统集成功能说明

## 概述

本系统已成功集成多个免费开放的学术数据库，实现了智能数据库选择和检索式转换功能，大大扩展了原有PubMed单一数据库的局限性。

## 完成的功能模块

### 1. 多数据库搜索引擎 (`multi_database_search.py`)

#### 支持的数据库：
- **OpenAlex** - 全学科领域，超过2.5亿篇学术文献
- **arXiv** - 物理学、数学、计算机科学等预印本论文
- **Semantic Scholar** - AI驱动的学术搜索，覆盖计算机科学、生物医学等
- **DOAJ** - 开放获取期刊目录，覆盖所有学科的高质量同行评议内容
- **Crossref** - 权威的学术元数据和引用信息

#### 主要功能：
- 并行搜索多个数据库
- 自动处理不同数据库的API限制
- 统一的搜索接口
- 错误处理和重试机制

### 2. 数据规范化模块 (`data_normalizer.py`)

#### 核心功能：
- **统一数据格式**：将不同数据库的返回格式统一为标准格式
- **字段映射**：标准化标题、作者、摘要、期刊等字段
- **数据清洗**：去除重复文献、规范化年份、DOI等
- **兼容性**：确保与现有PubMed逻辑完全兼容

#### 标准化字段：
```python
{
    'title': str,           # 标题
    'authors': list,        # 作者列表
    'abstract': str,        # 摘要
    'journal': str,         # 期刊名称
    'pub_year': int,        # 发表年份
    'doi': str,            # DOI
    'url': str,            # 文献链接
    'database': str,       # 来源数据库
    'journal_info': dict,  # 期刊信息
    'impact_factor': float, # 影响因子
    'jcr_quartile': str,   # JCR分区
    'cas_quartile': str,   # 中科院分区
    'relevance': float,    # 相关度
    'is_open_access': bool, # 是否开放获取
    'citation_count': int, # 引用次数
    'type': str,          # 文献类型
    'keywords': list      # 关键词
}
```

### 3. 智能搜索路由 (`intelligent_search_router.py`)

#### 核心功能：
- **智能数据库选择**：根据用户输入自动推荐最适合的数据库
- **检索式转换**：将自然语言查询转换为特定数据库的专业检索式
- **领域识别**：自动识别研究领域并优化搜索策略

#### 智能分析流程：
1. **用户输入分析**：使用DeepSeek AI分析用户查询内容
2. **领域识别**：识别医学、计算机科学、物理学等研究领域
3. **数据库推荐**：基于领域特点推荐最适合的数据库组合
4. **检索式优化**：为每个数据库生成专门优化的检索策略

#### 示例转换：

**用户输入**：`"糖尿病的药物治疗研究"`

**系统分析**：
- 检测领域：医学
- 推荐数据库：PubMed, OpenAlex, Semantic Scholar
- PubMed检索式：`"Diabetes Mellitus"[MeSH] AND "Drug Therapy"[MeSH] AND "Therapeutic Use"[Subheading]`
- OpenAlex检索式：`diabetes drug treatment therapy medication`
- Semantic Scholar检索式：`"diabetes mellitus" AND "drug therapy" AND treatment`

### 4. 集成搜索引擎 (`integrated_search.py`)

#### 功能特点：
- **统一接口**：提供统一的多数据库搜索API
- **智能路由**：自动选择最适合的数据库组合
- **结果合并**：智能去重和排序
- **性能优化**：并行搜索和批量处理

#### 搜索策略：
- **领域特定推荐**：
  - 医学领域：PubMed → OpenAlex → Semantic Scholar
  - 计算机科学：arXiv → Semantic Scholar → OpenAlex
  - 物理学：arXiv → OpenAlex → Semantic Scholar
  - 综合研究：OpenAlex → Semantic Scholar → Crossref

### 5. 新增API端点

#### `/api/analyze_query` (POST)
分析用户查询并推荐数据库和检索策略

**请求参数**：
```json
{
    "query": "用户查询内容"
}
```

**响应示例**：
```json
{
    "success": true,
    "query": "糖尿病的药物治疗研究",
    "detected_field": "医学",
    "recommended_databases": ["pubmed", "openalex", "semantic_scholar"],
    "search_strategies": {
        "pubmed": "\"Diabetes Mellitus\"[MeSH] AND \"Drug Therapy\"[MeSH]",
        "openalex": "diabetes drug treatment therapy",
        "semantic_scholar": "\"diabetes mellitus\" AND \"drug therapy\""
    },
    "analysis_info": {
        "analysis_text": "AI分析结果...",
        "selection_method": "ai_analysis"
    }
}
```

#### `/api/multi_database_search` (POST)
执行多数据库搜索

**请求参数**：
```json
{
    "query": "搜索查询",
    "databases": ["openalex", "arxiv"],  // 可选，不指定则自动推荐
    "field": "computer_science",         // 可选，研究领域
    "max_results": 600,                  // 可选，最大结果数
    "filters": {}                        // 可选，筛选条件
}
```

#### `/api/get_supported_databases` (GET)
获取支持的数据库列表

#### `/api/demo_multi_database` (GET)
演示多数据库功能

### 6. 演示功能 (`demo_integration.py`)

#### 演示内容：
- 数据库覆盖范围展示
- 智能路由功能演示
- 检索策略转换示例
- 完整的功能测试

#### 运行演示：
```bash
python demo_integration.py
```

## 技术特点

### 1. 智能化
- **AI驱动分析**：使用DeepSeek AI分析用户查询
- **自动数据库选择**：基于内容智能推荐最适合的数据库
- **检索式优化**：为不同数据库生成专门优化的检索策略

### 2. 兼容性
- **无缝集成**：与现有PubMed逻辑完全兼容
- **统一格式**：所有数据库结果统一为标准格式
- **向后兼容**：不影响现有功能的正常使用

### 3. 可扩展性
- **模块化设计**：每个数据库独立实现，易于添加新数据库
- **配置化**：数据库配置和优先级可灵活调整
- **API标准化**：统一的接口设计便于扩展

### 4. 性能优化
- **并行搜索**：多数据库并行查询提高效率
- **批量处理**：优化的批量数据处理
- **缓存机制**：智能缓存减少重复请求
- **错误恢复**：完善的错误处理和重试机制

## 使用场景

### 1. 跨学科研究
- 自动识别研究领域并推荐合适的数据库组合
- 获得更全面的文献覆盖

### 2. 前沿技术研究
- arXiv提供最新的预印本论文
- Semantic Scholar提供AI增强的语义搜索

### 3. 开放获取需求
- DOAJ提供高质量的开放获取文献
- 自动识别开放获取资源

### 4. 综合性研究
- OpenAlex提供全学科覆盖
- Crossref提供权威的元数据和引用信息

## 部署说明

### 1. 依赖安装
```bash
pip install requests xml beautifulsoup4 concurrent.futures
```

### 2. 配置文件
- 各数据库API配置（大部分为免费API）
- 嵌入模型配置（用于相关度计算）

### 3. 启动服务
系统会自动检测多数据库模块的可用性，如果模块不可用会回退到PubMed搜索。

## 总结

本次集成成功实现了：

1. ✅ **多数据库支持**：集成5个主要的免费学术数据库
2. ✅ **智能路由**：AI驱动的数据库选择和检索式转换
3. ✅ **数据规范化**：统一的数据格式，完全兼容现有逻辑
4. ✅ **演示功能**：完整的演示和API端点

系统现在能够：
- 根据用户输入智能选择最适合的数据库
- 将自然语言查询转换为专业检索式
- 提供比单一PubMed数据库更全面的文献覆盖
- 保持与现有界面和功能的完全兼容

这大大扩展了系统的适用范围，从原来仅限于生物医学领域扩展到全学科领域，为用户提供更智能、更全面的学术文献检索服务。
